# Gnet-Proxy 性能进阶任务 (连接预热池)

- `[ ]` **一期工程：配置与管理层**
  - `[ ]` 扩展 `config.RouteRule` 支持 `jump_start` 参数
  - `[ ]` 创建 `pkg/outbound/pool.go` 连接池管理器
  - `[ ]` 实现池子的健康检查与动态扩缩容
- `[ ]` **二期工程：核心注入**
  - `[ ]` 改造 `Dialer.Dial` 接入池化逻辑
  - `[ ]` 在 `main.go` 中启动全局预热协程
- `[ ]` **三期工程：稳定性调优**
  - `[ ]` 增加 `idle_timeout` 自动清理逻辑（待确认）
  - `[ ]` 单元测试验证池子的并发安全性
